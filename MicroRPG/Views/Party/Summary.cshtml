@model PartySummaryVM
@using MicroRPG.Models.Backstory
@{
    ViewData["Title"] = "Summary";
}
<div class="ViewHeader" id="summaryHeader">
    <h1>Party Summary</h1>
</div>
<div class="summaryContainer">

    <ul id="partyList">
        @for (int i = 0; i < Model.PlayerIDs.Length; i++)
        {
            <li class="player">
                <a asp-controller="Game" asp-action="_PlayerDetails" asp-route-id=@Model.PlayerIDs[i]>@Model.PlayerNames[i]</a>
            </li>
        }

    </ul>

    <div id="summaryFooter">
        <div id="summaryCreateBtn">
            <a asp-controller="Party" asp-action="Create">
                <input  id="addBtn" type=button value="Add Party Member"  />
            </a>
        </div>

        <div id="summaryContinueBtn">
            <a asp-controller="Party" asp-action="Backstory">
                <input id="continueBtn" type=button  value="Continue"  />
            </a>
        </div>
    </div>
</div>